# URL del backend de Vokter.
# Déjalo vacío: la app detecta sola la IP del PC desde el que Expo sirve el bundle
# (funciona igual en emulador y en teléfono físico).
# Solo escribe un valor si tu backend corre en otra máquina o en otro puerto.
EXPO_PUBLIC_API_URL=

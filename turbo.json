{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "env": ["API_ORIGIN"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {}
  }
}